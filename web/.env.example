# Leave blank behind Nginx. Use http://127.0.0.1:8000 only when running Nuxt directly.
NUXT_PUBLIC_API_BASE=
# Local development only. Must match the API's ADMIN_DEV_TOKEN.
NUXT_PUBLIC_ADMIN_DEV_TOKEN=
